class Bot {
    name = 'bot';
    constructor(name) {
        this.name = name;
    }

    chooseTrumpf(cards) {
        return card; // index??
    }

    chooseCard(center, hand) {
        return card; // index??
    }
}